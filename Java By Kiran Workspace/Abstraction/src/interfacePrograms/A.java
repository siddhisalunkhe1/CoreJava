package interfacePrograms;

public interface A
{
	 //Interface provides 100% abstraction
     //by default all the variables in interface are public, static and final i.e public static final int y=100;
	//we cannot define constructor in interface
	 //by default all the methods in interface are public and abstract
	 //interface is the contract between customer and service provider
	//interface can implement more than one interfaces
	//empty interface is called marker interface e.g serializable, cloneable
	//In interface static and default methods can be defined from 1.8 version
	//In interface private methods can be defined from 1.9 version
	
}
